/** 
 * There are three kinds of scope in JS, global scope, function scope and 
 * block scope. The difference between function scope and block scope, is 
 * that block scope includes everything inside code blocks { }, not just functions.
 */