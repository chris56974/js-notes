/** 
 * It's a behavioural design pattern in which an object is used to encapsulate all the information it needs 
 * to perform an action or trigger an event at a later time. 
 * The information is stuff like the method name, the object that owns the method and values for the method parameters.
 */
