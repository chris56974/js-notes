/** 
 * When you hide the implementation details from the user and give them a polished outside layer to work with. 
 * Con is it could be too simple, or too specific. 
 * It's different from the mediator pattern because it reveals existing functionality whereas the mediator "adds" 
 * functionality by combining existing functionality.
 */
