/** 
 * Structural design pattern where you have a group of objects 
 * that are all treated the same way (as a single instance of the same type). 
 * The intent is to compose objects into tree structures to represent part-whole hierarchies.
 */
