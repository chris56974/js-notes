/** 
 * It's when a class creates its dependencies but uses a service locator to do it. 
 * The service locator hides dependencies and you can't tell whether an object has its dependencies or not.
 */