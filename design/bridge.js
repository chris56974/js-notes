/** 
 * It's a structural desigIt's a structural design pattern that is meant to decouple an abstraction (interface) 
 * from its implementation (class) so that the two can vary independently. 
 * It uses encapsulation, aggregation and optionally inheritance to separate responsibilities into different classes.n pattern
 * that is meant to decouple an abstraction (interface) from its implementation (class) so that the two can vary independently. 
 * It uses encapsulation, aggregation and optionally inheritance to separate responsibilities into different classes.
 */
