/** 
 * A "typed" array is something like UInt8
 * new keyword is optional 
 */
var a = [],          // [] -- literal
    b = new Array()  // [] -- constructor object

var c = [3],         // [3]
    d = new Array(3) // ['', '', '']