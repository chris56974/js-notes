/** 
 * Memory leaks happen when you have an abundance of memory that isn't being released. 
 * Examples are stuff like too many global variables, too many event listeners that 
 * haven't been closed, or too many setInterval() or setTimeOuts that never end.
 */