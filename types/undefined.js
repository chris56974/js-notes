/** 
 * Undefined is a property of the global object, 
 * and its initial value is the undefined (primitive type). 
 * 
 * A variable that has not been assigned a value is of 
 * type undefined and returns undefined whenever we return it. 
 */
